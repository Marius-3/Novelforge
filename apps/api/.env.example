# mock 表示先不调用真实 AI，只返回示例数据，方便你检查项目是否能跑通。
# 要接 DeepSeek 时，把它改成 deepseek。
LLM_PROVIDER=mock

# 使用 DeepSeek 时填写。
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com

# 项目数据保存目录。
DATA_DIR=./data
